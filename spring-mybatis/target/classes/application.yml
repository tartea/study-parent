server:
  port: 8080

spring:
  # 数据源
  datasource:
    username: root
    password: 12345
    url: jdbc:mysql://localhost:3306/mybatis-base?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis配置
mybatis:
  # 配置文件所在位置
  config-location: classpath:mybatis-config.xml
  # 映射文件所在位置
  mapper-locations: classpath:mappers/*Mapper.xml

mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
